<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.myhome.mybatis.mapper.UserMapper">

		<!-- 지니 -->
	
	<select id = "isId" resultType = "user">
		SELECT u.email, u.name, u.phone, c.name
		AS company_name
		FROM users u
		INNER JOIN company c ON u.id = c.user_id
		WHERE u.email = #{email}
	</select>
	
	<update id = "update" parameterType = "user">
		update users
		set name = #{name},
   		phone = #{phone}
		WHERE email = #{email}
		
	</update>

	<select id="checkPwd">
		select password 
		from users
		where email = #{email}
	</select>
	
	<update id="updatePwd">
		update users
		set password =#{newPwd}
		where email = #{email}
	</update>
	
	<!-- /지니 -->


	<delete id="delete" parameterType="String">
		delete from user
		where id =#{id}
	</delete>
	
	
	<!-- #########################################주영###################################### START  -->
	<select id="selectByMail" resultType="user" parameterType="String">
		select * from users where email = #{email}
	</select>
	
	<select id="getUserId" resultType="int">
		select id from users where email = #{email}
	</select>
	
	<insert id="insert" parameterType="user">
		insert into users
		(id,email,password,name,company_invited)
		values
		(user_id.nextval,#{email},#{password},#{name},1)
	</insert> 
	
	<!-- #########################################주영###################################### END  -->
</mapper>