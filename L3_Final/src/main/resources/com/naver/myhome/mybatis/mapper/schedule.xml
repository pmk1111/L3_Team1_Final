<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.myhome.mybatis.mapper.ScheduleMapper">
	<!-- Insert -->
	<insert id="insertSchedule">
		<selectKey order="BEFORE" keyProperty="id" resultType="int">
			select schedule_id.nextval from dual
		</selectKey>
		INSERT INTO SCHEDULE VALUES(#{id}, #{user_id},#{subject}, #{start_date}, #{end_date} ,
		 #{category} , #{place}, #{content})
	</insert>
	 
	<!-- Update -->
	 <update id="updateSchedule" parameterType="int">
        UPDATE SCHEDULE SET user_id = #{user_id},
        subject = #{subject},
        start_date = #{start_date},
        end_date = #{end_date},
        category = #{category},
        place = #{place},
        content =  #{content}
        WHERE id =  #{id}
    </update>

    <!-- Delete -->
    <delete id="deleteSchedule" parameterType="int">
       DELETE FROM SCHEDULE WHERE id= # {id}
    </delete>
    
</mapper>