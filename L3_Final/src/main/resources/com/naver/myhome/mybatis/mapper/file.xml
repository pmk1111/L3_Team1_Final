<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.myhome.mybatis.mapper.FileMapper">
	<sql id="fileColumns">
		file_id,
		issue_id,
		original_name,
		save_name,
		file_size,
		delete_yn,
		created_date,
		deleted_date
	</sql>

  <insert id="uploadFile" parameterType="list">
  INSERT INTO files (
    <include refid="fileColumns" />
  )
  SELECT
    file_id.nextval, A.*
  FROM (
    <foreach item="file" index="index" collection="list" separator="UNION ALL">
      SELECT
        #{file.issue_id},
        #{file.original_name},
        #{file.save_name},
        #{file.file_size},
        0,
        SYSDATE,
        NULL
      FROM DUAL
    </foreach>
  ) A
</insert>

	<!-- 파일 리스트 조회 -->
	<select id="getFileList" parameterType="int"
		resultType="com.naver.myhome.domain.Files">
		SELECT
		*
		FROM files
		WHERE delete_yn = 0
		AND issue_id = #{num}
		ORDER BY file_id
	</select>


	<!-- 파일 리스트 조회 -->
	<select id="findAllByIds" parameterType="list"
		resultType="com.naver.myhome.domain.Files">
		SELECT
		<include refid="fileColumns" />
		FROM
		tb_file
		WHERE
		delete_yn = 0
		AND id IN
		<foreach item="id" collection="list" open="(" separator=","
			close=")">
			#{id}
		</foreach>
		ORDER BY
		id
	</select>


	<!-- 파일 삭제 -->
	<delete id="deleteAllByIds" parameterType="list">
		UPDATE files
		SET
		delete_yn = 1
		, deleted_date = NOW()
		WHERE
		id IN
		<foreach item="id" collection="list" open="(" separator=","
			close=")">
			#{id}
		</foreach>
	</delete>

</mapper>
