<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.myhome.mybatis.mapper.MytotalworkMapper">
	

	<select id="mytotallist" resultType="com.naver.myhome.domain.Mytotalwork">
    
   SELECT P.title AS project_title, I.issue_subject AS issue_title, I.issue_status
FROM project P
JOIN issue I ON P.num = I.project_id
WHERE I.create_user = #{I.create_user}
   
 
  </select>
  

</mapper>